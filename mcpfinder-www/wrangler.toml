name = "mcp-www"
# main = "src/index.js" # Entry point might not be needed if using Pages, or might be different. Please verify.
compatibility_date = "2024-05-04" # Update to current date or init date

# If using Cloudflare Pages, configure 'pages_build_output_dir = "public"' or similar
# pages_build_output_dir = "../public" # Adjust path relative to this wrangler.toml

# If serving assets via a Worker + KV Assets:
# [[kv_namespaces]]
# binding = "MCP_STATIC_CONTENT" # Using MCP_ prefix
# id = "YOUR_WWW_KV_ID_HERE"
# preview_id = "YOUR_WWW_KV_PREVIEW_ID_HERE"

# [site]
# bucket = "../public" # Path to the directory of static assets relative to wrangler.toml 